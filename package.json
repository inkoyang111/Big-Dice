{
  "name": "liars-dice-miniprogram",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check app.js && node --check utils/dice-game.js && node --check utils/audio-player.js && node --check pages/index/index.js",
    "prepare:assets": "node scripts/prepare-runtime-assets.js"
  },
  "devDependencies": {
    "sharp": "^0.35.3"
  },
  "engines": {
    "node": ">=24"
  }
}
