# 大话骰小程序 V2 代码交接

更新时间：2026-08-20

## 1. 当前结论

- V1 已正式发布。
- V1 基线：`1993cd4 chore: optimize mini program release assets`。
- V2 开发分支：`codex/v2-double-tap-ones`。
- V2 功能提交：`900fa67 feat: add lucky double-tap roll`。
- V2 分支已推送到 GitHub，尚未合并到 `main`。
- V2 已通过 20 项自动化测试、语法检查和微信开发者工具验证。
- AppID：`wx7f6aa6ea7f4a818c`。
- 首页：`pages/index/index`。

GitHub 仓库：`https://github.com/inkoyang111/Big-Dice.git`

## 2. V2 新增功能

- 用户从第一次有效点击到第二次有效点击的间隔小于或等于 300ms 时，触发隐藏双击规则；恰好 300ms 有效。
- “摇一摇”和“再摇一次”均支持触发。
- 触发后，本轮 5 颗骰子至少包含 3 个 1，允许最终出现 3、4 或 5 个 1。
- 保底 1 的位置由随机逻辑决定，不固定在预设位置。
- 双击只产生一轮结果，动画和音效各播放一次，不显示文字、Toast、特殊按钮态或额外动效。
- 三次及更多快速点击仍只产生一轮结果；隐藏触发成立后的点击不再改变结果。
- 超过 300ms 的第二次点击不触发特殊规则，当前轮结果保持普通随机结果。

完整产品规则和验收标准见 `需求文档.md` 第 15 章。

## 3. 关键实现

- `pages/index/index.js`：记录首次点击时间，在摇骰状态识别 300ms 内的第二次点击，不重复启动计时器或音效。
- `utils/dice-game.js`：在当前隐藏结果中随机选择非 1 位置，将 1 的数量补足到至少 3 个。
- `tests/dice-game.test.js`：验证至少 3 个 1 以及保底位置不是固定写死。
- `tests/index-page.test.js`：验证 300ms 边界、超时、三连击、“再摇一次”、单轮动画和单次音效。

V1 的四态状态机、开合盖隐私、前后台处理和音频降级逻辑保持不变。

## 4. 验证结果

```powershell
npm test
npm run check
```

- 自动化测试：20 项全部通过。
- JavaScript 语法检查：通过。
- 微信开发者工具独立工作区测试：用户已确认通过。

正式发布前仍需完成：

1. iOS 和 Android 真机预览。
2. 普通单击、300ms 双击、“再摇一次”、三连击和前后台切换复测。
3. 上传体验版并由体验成员验收。
4. 提交微信审核并发布。

## 5. Git 与工作区状态

- 当前分支从 V1 基线创建，V2 功能提交已推送至 `origin/codex/v2-double-tap-ones`。
- 下一步应提交本次 README、交接文档和项目配置更新，再合并至 `main`。
- `project.private.config.json` 是个人配置，保持 Git 忽略。
- `project.config.json` 包含微信开发者工具生成的正式项目设置；提交前应确认 AppID 和打包排除项不变。
- 不要使用未经核对的 `git add --all`。

## 6. 下一步

1. 提交文档与 `project.config.json` 更新。
2. 将 `codex/v2-double-tap-ones` 合并到 `main` 并推送 GitHub。
3. 在原项目目录拉取或合并后的 `main` 中完成最终微信开发者工具验证。
4. 上传 V2 体验版，通过验收后提交审核和发布。
