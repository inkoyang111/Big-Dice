# 「大话骰」MVP 测试用例

依据：《需求文档.md》V1.0 及产品经理对待确认项的最终决策。

状态说明：INITIAL（初始）、ROLLING（摇骰中）、READY（已摇待查看）、OPEN（已开盖）。

## 一、自动化测试清单

| 编号 | 场景 | 前置条件与步骤 | 预期结果 | 对应测试 |
| --- | --- | --- | --- | --- |
| AT-01 | 随机结果合法性 | 自动生成 10,000 轮 | 共检查 50,000 个结果；每轮恰好 5 个；每个均为 1～6 整数；任一非法即失败 | `tests/dice-game.test.js` |
| AT-02 | 随机边界及重复 | 固定随机源为 0、0.999999 或相同值 | 分别映射为 1、6；允许同轮重复 | `tests/dice-game.test.js` |
| AT-03 | 摇骰防重复 | ROLLING 时再次摇骰和点击骰盅 | 不生成新结果、不打开骰盅，随机源总共只调用 5 次 | `tests/dice-game.test.js` |
| AT-04 | 开合盖保持结果 | 完成摇骰后反复开合 | READY/OPEN 正确切换，同一组结果不变 | `tests/dice-game.test.js` |
| AT-05 | 再摇一次 | OPEN 时重新摇骰 | 立即进入 ROLLING 并覆盖旧结果 | `tests/dice-game.test.js` |
| AT-06 | 标准摇骰时长 | 点击摇骰并检查计时器，触发计时完成 | 固定 1500ms；之后进入 READY 并显示指定提示 | `tests/index-page.test.js` |
| AT-07 | 提示显示条件 | READY 开盖、合盖、重新摇骰 | 开盖/重新摇骰时指定提示消失；合盖后重新显示 | `tests/index-page.test.js` |
| AT-08 | 初始 Toast 防重复 | INITIAL 立即连续点击骰盅 3 次 | 只展示一次“请先摇骰”，时长 1500ms | `tests/index-page.test.js` |
| AT-09 | OPEN 后台隐私 | OPEN 时调用页面隐藏生命周期 | 立即变为 READY；结果保留且不可见 | `tests/index-page.test.js` |
| AT-10 | ROLLING 前后台一致性 | 摇骰后隐藏并返回页面 | 仍为同一组结果；不重新播放音效 | `tests/index-page.test.js` |
| AT-11 | 音效异常降级 | 模拟创建、播放和销毁音频失败 | 不向页面抛错，不阻断摇骰 | `tests/audio-player.test.js` |

## 二、微信开发者工具／真机手工测试

| 编号 | 优先级 | 场景 | 前置条件 | 步骤 | 预期结果 |
| --- | --- | --- | --- | --- | --- |
| MT-01 | P0 | 实测摇骰耗时 | 微信开发者工具性能面板或真机录屏计时 | 点击“摇一摇”，记录点击生效至进入 READY 的时间，重复 10 次 | 每次均在 1.4～1.7 秒内，目标值 1.5 秒 |
| MT-02 | P0 | READY 提示位置和文案 | 完成一次摇骰 | 等待动画结束 | 骰盅右上方显示“点击骰盅开盖查看”，没有独立开盖按钮 |
| MT-03 | P1 | 一次性提示动效 | 完成一次摇骰并录屏 | 逐帧检查 READY 时骰盅动作 | 骰盅只轻微上浮回落一次；总时长不超过 0.6 秒；不循环 |
| MT-04 | P0 | 提示消失与重现 | READY 状态 | 依次开盖、合盖、再摇一次、重新加载页面 | 开盖和再摇时提示消失；合盖后重现；页面重载后不显示 READY 提示 |
| MT-05 | P1 | Toast 节流边界 | INITIAL 状态 | 0 秒连续点击；1.9 秒再次点击；超过 2 秒再次点击 | 前 2 秒窗口最多一次且不刷新、不叠加；超过 2 秒可再次展示；每次约 1.5 秒 |
| MT-06 | P0 | 点击开盖区域 | READY 状态 | 点击骰盅主体不同可见区域 | 每次有效点击只触发一次开盖动画，最终进入 OPEN |
| MT-07 | P0 | 点击合盖区域 | OPEN 状态 | 点击已移开的骰盅盖不同可见区域 | 每次有效点击只触发一次合盖动画，最终进入 READY |
| MT-08 | P1 | MVP 无拖动 | READY 和 OPEN 状态 | 分别向上、向下拖动，不产生点击释放 | 拖动不是开合盖依赖方式；点击始终可完成开合盖；不出现状态错乱 |
| MT-09 | P0 | ROLLING 后台超过时长 | 点击摇骰后立即记录结果调试值并切后台，累计超过 1.5 秒后返回 | 返回页面 | 立即进入 READY；仍是点击时生成的同一组结果；音效不重播 |
| MT-10 | P0 | ROLLING 后台不足时长 | 点击摇骰后约 0.5 秒切后台，约 0.5 秒后返回 | 观察剩余动画并计时 | 继续约 0.5 秒剩余动画；累计满 1.5 秒进入 READY；不重新生成结果、不重播音效 |
| MT-11 | P0 | OPEN 后台隐私保护 | OPEN 且记录 5 颗骰子 | 切入后台再返回，然后开盖核对 | 切后台立即自动合盖；返回为 READY 且点数不可见；再次开盖仍为原结果 |
| MT-12 | P1 | INITIAL/READY 后台保持 | 分别处于 INITIAL、READY | 切后台再返回 | 页面未销毁时分别保持 INITIAL、READY |
| MT-13 | P1 | 页面销毁重载 | 任意已有结果状态 | 关闭/销毁页面后重新进入 | 回到 INITIAL；骰盅关闭；无历史结果；主按钮为“摇一摇” |
| MT-14 | P0 | 快速点击防重复 | 分别在 ROLLING、开盖动画、合盖动画中快速点击 | 连续点击主按钮或骰盅 | 不叠加动画、音效或 Toast；一次点击只触发一次状态变化；不泄露点数 |
| MT-15 | P0 | 合盖防泄露 | READY、ROLLING、合盖动画结束状态 | 录屏逐帧检查骰盅区域 | 不出现点数闪现、残影、阴影穿透或可推测结果 |
| MT-16 | P1 | 音效失败 | 真机静音或模拟音频加载/播放失败 | 完成摇骰和开盖 | 无阻断报错；动画、结果生成和开盖正常完成 |
| MT-17 | P1 | 多尺寸显示 | iOS/Android 常见小屏和长屏真机 | 完成摇骰并开盖 | 5 颗骰子完整、清晰且不重叠；核心控件不被安全区遮挡 |

## 三、验收口径

- 自动化测试通过仅证明纯逻辑及可模拟页面行为通过，不代替微信开发者工具和真机验收。
- 手工用例在实际执行前状态均为“用例已准备”，不得标记为“实现已通过”。
- 随机性检查不设置频率比例或卡方阈值；重复结果、新旧两轮完全相同均合法。
